<style>
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding: 1rem;
  }
  
  .floating-clouds {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }
  
  .cloud-1 {
    position: absolute;
    top: 5rem;
    left: 2.5rem;
    width: 8rem;
    height: 4rem;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 9999px;
    filter: blur(1rem);
    animation: float 3s ease-in-out infinite;
  }
  
  .cloud-2 {
    position: absolute;
    top: 10rem;
    right: 5rem;
    width: 12rem;
    height: 6rem;
    background: rgba(230, 230, 250, 0.2);
    border-radius: 9999px;
    filter: blur(2rem);
    animation: float 3s ease-in-out infinite;
    animation-delay: 1s;
  }
  
  .cloud-3 {
    position: absolute;
    bottom: 10rem;
    left: 25%;
    width: 10rem;
    height: 5rem;
    background: rgba(255, 182, 193, 0.2);
    border-radius: 9999px;
    filter: blur(1rem);
    animation: float 3s ease-in-out infinite;
    animation-delay: 2s;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  @keyframes pulseGlow {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
  }
  
  .fade-in-up { animation: fadeInUp 0.6s ease-out; }
  .pulse-glow { animation: pulseGlow 2s ease-in-out infinite; }
  .animate-float { animation: float 3s ease-in-out infinite; }
</style>

<section class="hero-section">
  <!-- Nuages flottants en arrière-plan -->
  <div class="floating-clouds">
    <div class="cloud-1"></div>
    <div class="cloud-2"></div>
    <div class="cloud-3"></div>
  </div>

  <div style="max-width: 1200px; margin: 0 auto; position: relative; z-index: 10; text-align: center;">
    <div style="max-width: 1024px; margin: 0 auto;">
      <!-- Badge "Innovation technologique" -->
      <div style="display: inline-flex; align-items: center; gap: 0.5rem; background: rgba(255, 255, 255, 0.4); backdrop-filter: blur(12px); padding: 0.5rem 1rem; border-radius: 9999px; border: 1px solid rgba(255, 255, 255, 0.3); margin-bottom: 2rem;" class="fade-in-up">
        <span style="width: 0.5rem; height: 0.5rem; background: var(--sky-blue); border-radius: 50%;" class="pulse-glow"></span>
        <span style="font-size: 0.875rem; font-weight: 500; color: rgb(55, 65, 81);">Innovation technologique ⚡</span>
      </div>

      <!-- Titre principal avec gradient -->
      <h1 style="font-size: clamp(2.5rem, 8vw, 4.5rem); font-weight: 700; margin-bottom: 1.5rem; line-height: 1.1;" class="fade-in-up">
        <span class="gradient-text">Contrôle tout</span>
        <br />
        <span style="color: rgb(31, 41, 55);">d'un simple geste !</span>
      </h1>

      <!-- Sous-titre -->
      <p style="font-size: clamp(1.125rem, 2vw, 1.25rem); color: rgb(75, 85, 99); margin-bottom: 2rem; max-width: 32rem; margin-left: auto; margin-right: auto;" class="fade-in-up">
        La bague connectée qui révolutionne ton quotidien digital. 
        Swipe, contrôle et capture sans jamais toucher ton écran 💍
      </p>

      <!-- Image produit principale -->
      <div style="position: relative; margin-bottom: 3rem;" class="fade-in-up">
        <div style="position: relative; display: inline-block;">
          {% if section.settings.hero_image %}
            <img 
              src="{{ section.settings.hero_image | image_url: width: 400 }}"
              alt="{{ section.settings.hero_image.alt | escape }}"
              style="width: clamp(16rem, 30vw, 24rem); height: auto; margin: 0 auto; border-radius: 1.5rem; box-shadow: 0 25px 50px rgba(135, 206, 235, 0.2);"
              class="animate-float"
            />
          {% else %}
            <img 
              src="https://images.unsplash.com/photo-1515377905703-c4788e51af15?auto=format&fit=crop&w=400&q=80"
              alt="Zen Ring - Bague connectée Bluetooth"
              style="width: clamp(16rem, 30vw, 24rem); height: auto; margin: 0 auto; border-radius: 1.5rem; box-shadow: 0 25px 50px rgba(135, 206, 235, 0.2);"
              class="animate-float"
            />
          {% endif %}
          <!-- Effet de brillance -->
          <div style="position: absolute; inset: 0; background: linear-gradient(135deg, transparent, rgba(255, 255, 255, 0.2), transparent); border-radius: 1.5rem;"></div>
        </div>
      </div>

      <!-- Prix et CTA -->
      <div style="display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1.5rem; margin-bottom: 2rem;" class="fade-in-up">
        <div style="text-align: center;">
          <div style="font-size: clamp(2rem, 5vw, 2.5rem); font-weight: 700; color: rgb(31, 41, 55); margin-bottom: 0.25rem;">
            <span style="font-size: 1.125rem; color: rgb(107, 114, 128); text-decoration: line-through; margin-right: 0.5rem;">29€</span>
            19€
          </div>
          <div style="font-size: 0.875rem; color: rgb(107, 114, 128);">Livraison gratuite 🚚</div>
        </div>
        
        <a href="{{ collections['zen-ring'].products.first.url | default: '/products/zen-ring' }}" class="btn-cloud" style="font-size: 1.125rem;">
          Je veux ma Zen Ring ⚡
          <span style="transition: transform 0.2s;">→</span>
        </a>
      </div>

      <!-- Garanties -->
      <div style="display: flex; flex-wrap: wrap; justify-content: center; gap: 1.5rem; font-size: 0.875rem; color: rgb(107, 114, 128);" class="fade-in-up">
        <div style="display: flex; align-items: center; gap: 0.5rem;">
          <span style="color: #10B981;">✓</span>
          Garantie 30 jours
        </div>
        <div style="display: flex; align-items: center; gap: 0.5rem;">
          <span style="color: #10B981;">✓</span>
          Paiement sécurisé
        </div>
        <div style="display: flex; align-items: center; gap: 0.5rem;">
          <span style="color: #10B981;">✓</span>
          Stock limité
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    }
  ]
}
{% endschema %}