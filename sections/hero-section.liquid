{{ 'hero-section.css' | asset_url | stylesheet_tag }}

<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-blue-50 via-white to-pink-50">
  <!-- Animated Background Elements -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute -top-40 -right-40 w-80 h-80 bg-blue-200 rounded-full opacity-30 animate-float"></div>
    <div class="absolute -bottom-32 -left-32 w-96 h-96 bg-pink-200 rounded-full opacity-20 animate-float-delay"></div>
    <div class="absolute top-1/2 left-1/4 w-64 h-64 bg-purple-200 rounded-full opacity-25 animate-pulse-soft"></div>
  </div>

  <div class="container mx-auto px-4 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center max-w-6xl mx-auto">
      <!-- Left Content -->
      <div class="text-center lg:text-left space-y-8 animate-fade-in-up">
        <div class="inline-block bg-white/80 backdrop-blur-sm rounded-full px-6 py-2 border border-white/20 shadow-lg">
          <span class="text-sm font-medium text-gray-700">🔥 Offre limitée - Stock restant: 47</span>
        </div>

        <h1 class="text-5xl lg:text-6xl font-bold leading-tight">
          <span class="bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 bg-clip-text text-transparent">
            Contrôle tout d'un simple geste !
          </span>
        </h1>

        <p class="text-xl text-gray-600 leading-relaxed max-w-lg">
          La <strong>Zen Ring</strong> révolutionne votre quotidien. Contrôlez vos appareils connectés, 
          gérez vos notifications et simplifiez votre vie avec cette bague intelligente ultra-discrète.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 items-center justify-center lg:justify-start">
          <a href="{{ collections['zen-ring'].products.first.url | default: '/products/zen-ring' }}" 
             class="group relative bg-gradient-to-r from-blue-500 to-purple-600 text-white px-8 py-4 rounded-2xl font-bold text-lg shadow-2xl transform hover:scale-105 transition-all duration-300 overflow-hidden">
            <span class="relative z-10">Je veux ma Zen Ring ⚡</span>
            <div class="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-500 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </a>
          
          <div class="flex items-center space-x-2 text-gray-600">
            <div class="flex">
              {% for i in (1..5) %}
                <svg class="w-5 h-5 fill-current text-yellow-400" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                </svg>
              {% endfor %}
            </div>
            <span class="text-sm">487 avis clients</span>
          </div>
        </div>

        <!-- Guarantees -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 pt-8">
          <div class="flex items-center space-x-3 bg-white/60 backdrop-blur-sm rounded-xl p-4 border border-white/20">
            <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700">Garantie 30 jours</span>
          </div>
          
          <div class="flex items-center space-x-3 bg-white/60 backdrop-blur-sm rounded-xl p-4 border border-white/20">
            <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
              <svg class="w-5 h-5 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"/>
              </svg>
            </div>
            <span class="text-sm font-medium text-gray-700">Paiement sécurisé</span>
          </div>
        </div>
      </div>

      <!-- Right Content - Product Image -->
      <div class="relative animate-fade-in-up" style="animation-delay: 0.2s;">
        <div class="relative">
          <div class="absolute inset-0 bg-gradient-to-r from-blue-400 to-purple-500 rounded-full opacity-20 blur-3xl scale-75"></div>
          {% if section.settings.hero_image %}
            <img 
              src="{{ section.settings.hero_image | image_url: width: 600 }}"
              alt="{{ section.settings.hero_image.alt | escape }}"
              class="relative z-10 max-w-full h-auto mx-auto drop-shadow-2xl"
            />
          {% else %}
            <img 
              src="{{ 'zen-ring-hero.png' | asset_url }}"
              alt="Zen Ring - Bague connectée révolutionnaire"
              class="relative z-10 max-w-full h-auto mx-auto drop-shadow-2xl"
            />
          {% endif %}
          
          <!-- Floating elements -->
          <div class="absolute -top-8 -right-8 w-16 h-16 bg-white rounded-full shadow-lg flex items-center justify-center animate-bounce">
            <span class="text-2xl">⚡</span>
          </div>
          
          <div class="absolute -bottom-4 -left-4 w-12 h-12 bg-gradient-to-r from-pink-400 to-purple-400 rounded-full shadow-lg animate-pulse"></div>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    }
  ]
}
{% endschema %}