<style>
  .cta-section {
    padding: 5rem 1rem;
    position: relative;
    overflow: hidden;
  }
  
  .cta-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(135, 206, 235, 0.1), rgba(230, 230, 250, 0.1), rgba(255, 182, 193, 0.1));
  }
  
  .cta-cloud-1 {
    position: absolute;
    top: 2.5rem;
    left: 25%;
    width: 15rem;
    height: 7.5rem;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    filter: blur(2rem);
    animation: float 3s ease-in-out infinite;
  }
  
  .cta-cloud-2 {
    position: absolute;
    bottom: 5rem;
    right: 25%;
    width: 20rem;
    height: 10rem;
    background: rgba(230, 230, 250, 0.2);
    border-radius: 50%;
    filter: blur(3rem);
    animation: float 3s ease-in-out infinite;
    animation-delay: 1.5s;
  }

  .urgency-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.2);
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    margin-bottom: 2rem;
    animation: pulse-soft 2s ease-in-out infinite;
  }
  
  .urgency-dot {
    width: 0.5rem;
    height: 0.5rem;
    background: #EF4444;
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }
  
  .urgency-text {
    color: #B91C1C;
    font-weight: 500;
    font-size: 0.875rem;
  }

  .cta-title {
    font-size: clamp(2rem, 8vw, 4rem);
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }
  
  .cta-subtitle {
    font-size: clamp(1rem, 4vw, 1.25rem);
    color: rgb(107, 114, 128);
    margin-bottom: 2rem;
    max-width: 32rem;
    margin-left: auto;
    margin-right: auto;
    padding: 0 1rem;
  }

  .offer-card {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(12px);
    border-radius: 1.5rem;
    padding: 2rem;
    margin-bottom: 2rem;
    max-width: 32rem;
    margin-left: auto;
    margin-right: auto;
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 8px 32px rgba(135, 206, 235, 0.1);
  }
  
  .price-section {
    text-align: center;
    margin-bottom: 1.5rem;
  }
  
  .launch-price {
    font-size: 0.875rem;
    color: rgb(107, 114, 128);
    margin-bottom: 0.5rem;
  }
  
  .price-display {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }
  
  .old-price {
    font-size: clamp(1.25rem, 4vw, 1.5rem);
    color: rgb(156, 163, 175);
    text-decoration: line-through;
  }
  
  .current-price {
    font-size: clamp(2rem, 6vw, 2.5rem);
    font-weight: 700;
    background: linear-gradient(135deg, #87CEEB, #8B5CF6, #FFB6C1);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
  }
  
  .discount-badge {
    background: #EF4444;
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 600;
  }
  
  .free-shipping {
    font-size: 0.875rem;
    color: rgb(107, 114, 128);
  }

  .countdown-card {
    background: linear-gradient(135deg, rgba(254, 202, 202, 0.5), rgba(253, 186, 116, 0.5));
    border: 1px solid rgba(239, 68, 68, 0.2);
    border-radius: 1rem;
    padding: 1.5rem;
    max-width: 24rem;
    margin: 0 auto;
  }
  
  .countdown-title {
    color: #B91C1C;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  
  .countdown-time {
    font-size: 1.5rem;
    font-weight: 700;
    color: #991B1B;
    font-family: 'Courier New', monospace;
  }
  
  .countdown-labels {
    font-size: 0.75rem;
    color: #DC2626;
  }

  .footer-simple {
    margin-top: 4rem;
    text-align: center;
    color: rgb(107, 114, 128);
    font-size: 0.875rem;
  }
  
  .footer-border {
    border-top: 1px solid rgba(209, 213, 219, 0.5);
    padding-top: 2rem;
  }
  
  .footer-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 1rem;
  }
  
  .footer-link {
    color: rgb(107, 114, 128);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  
  .footer-link:hover {
    color: #87CEEB;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  @keyframes pulse-soft {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }
</style>

<section class="cta-section">
  <!-- Fond dégradé dramatique -->
  <div class="cta-bg"></div>
  
  <!-- Nuages animés -->
  <div style="position: absolute; inset: 0; pointer-events: none;">
    <div class="cta-cloud-1"></div>
    <div class="cta-cloud-2"></div>
  </div>

  <div style="max-width: 1200px; margin: 0 auto; position: relative; z-index: 10;">
    <div style="max-width: 64rem; margin: 0 auto; text-align: center;">
      <!-- Badge urgence -->
      <div class="urgency-badge">
        <span class="urgency-dot"></span>
        <span class="urgency-text">🔥 Stock limité - Plus que quelques pièces</span>
      </div>

      <!-- Titre principal -->
      <h2 class="cta-title">
        Prêt à contrôler ton <span class="gradient-text">monde</span> ?
      </h2>

      <p class="cta-subtitle">
        Rejoins la révolution de la technologie portable. Ton quotidien ne sera plus jamais le même ⚡
      </p>

      <!-- Offre spéciale -->
      <div class="offer-card">
        <div class="price-section">
          <div class="launch-price">Prix de lancement ✨</div>
          <div class="price-display">
            <span class="old-price">29€</span>
            <span class="current-price">19€</span>
            <span class="discount-badge">-34%</span>
          </div>
          <div class="free-shipping">+ Livraison gratuite en France 🚚</div>
        </div>

        <!-- Bouton principal -->
        <a href="{{ collections['zen-ring'].products.first.url | default: '/products/zen-ring' }}" class="btn-cloud" style="display: inline-flex; align-items: center; gap: 0.5rem; font-size: clamp(1rem, 3vw, 1.25rem); font-weight: 700; padding: 1rem 2rem; border-radius: 9999px; width: 100%; justify-content: center; margin-bottom: 1rem; text-decoration: none; color: white; white-space: nowrap;">
          Je veux ma Zen Ring ⚡
          <span style="transition: transform 0.2s;">💍</span>
        </a>

        <div style="font-size: 0.75rem; color: rgb(107, 114, 128);">
          Paiement 100% sécurisé • Garantie satisfait ou remboursé 15 jours
        </div>
      </div>

      <!-- Countdown timer -->
      <div class="countdown-card">
        <div class="countdown-title">⏰ Offre limitée dans le temps !</div>
        <div class="countdown-time" id="countdown-timer">23:45:12</div>
        <div class="countdown-labels">Heures • Minutes • Secondes</div>
      </div>
    </div>
  </div>

  <!-- Footer simple -->
  <footer class="footer-simple">
    <div class="footer-border">
      <p>&copy; 2024 Zen Ring - Contrôle ton monde ⚡</p>
      <div class="footer-links">
        <a href="#" class="footer-link">Mentions légales</a>
        <a href="#" class="footer-link">CGV</a>
        <a href="#" class="footer-link">Contact</a>
      </div>
    </div>
  </footer>
</section>

<script>
// Countdown timer functionality
(function() {
  function updateCountdown() {
    const now = new Date().getTime();
    const endTime = now + (23 * 60 * 60 * 1000) + (45 * 60 * 1000) + (12 * 1000);
    
    setInterval(function() {
      const current = new Date().getTime();
      const distance = endTime - current;
      
      const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);
      
      const timer = document.getElementById('countdown-timer');
      if (timer) {
        timer.innerHTML = 
          String(hours).padStart(2, '0') + ':' +
          String(minutes).padStart(2, '0') + ':' +
          String(seconds).padStart(2, '0');
      }
    }, 1000);
  }
  
  document.addEventListener('DOMContentLoaded', updateCountdown);
})();
</script>

{% schema %}
{
  "name": "CTA Section",
  "settings": []
}
{% endschema %}